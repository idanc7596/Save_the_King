#pragma once

//the third parameter written in the level .txt file is the time limit.
//if there is no time limit, the parameter there is 0:
const int NO_TIME_LIMIT = 0;

//window dimensions:
const int WINDOW_HEIGHT = 960,
WINDOW_WIDTH = 1280;